import { Box, Input } from "@chakra-ui/react";
import { react } from "react";

function Discussion(object) {
  return (
    <Box>
      <h2>{object.title}</h2>
    </Box>
  );
}

export default Discussion;
